/**
 * @stigmer/resource-api — proto-first resource API pipeline.
 *
 * TypeScript edition of the Stigmer request-pipeline commons (Java:
 * stigmer-cloud/backend/libs/java/grpc/grpc-request; Go:
 * stigmer/backend/libs/go/grpc/request/pipeline). The pipeline API
 * (envelope, operations, store port, error contract) is exported here;
 * Postgres-specific pieces live under `@stigmer/resource-api/postgres`.
 */

export {};
